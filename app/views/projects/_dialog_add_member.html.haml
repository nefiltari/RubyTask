%p Add Organisation Members:  
%p
  %table
    - @member.each do |f|
      %tr{member_id:f.name}
        %td
          %span{member_id:f.name}= f.realname
        %td
          %button{onClick:"projects_remove_member_with_id(#{f.id.to_i});"} add